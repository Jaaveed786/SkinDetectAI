# ============================================================
# SkinDetect.AI - Complete Backend Requirements
# Architecture: FastAPI + SQLite + TensorFlow/EfficientNet-B7
# Last updated: 2026-04-09 (security patch — all vulnerabilities fixed)
# ============================================================

# --- Core API Framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic[email]==2.10.3
python-multipart==0.0.12
python-dotenv==1.0.1
httpx==0.28.0
aiofiles==24.1.0

# --- Security / Authentication (JWT + Password Hashing) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- AI / Deep Learning Engine ---
tensorflow==2.17.0
keras==3.6.0
scipy==1.14.1
scikit-learn==1.5.2

# --- Computer Vision (Image Processing + Grad-CAM) ---
Pillow==11.0.0
numpy==1.26.4
opencv-python-headless==4.10.0.84
matplotlib==3.9.3

# --- PDF Report Generation ---
reportlab==4.2.5

# --- Database Drivers ---
# SQLite: Built-in (no install needed)
# PostgreSQL (Users DB)
asyncpg==0.30.0
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
# MongoDB (Scans & Images DB)
motor==3.6.0
pymongo==4.10.1

# --- Caching / Async Task Queue (Redis + Celery) ---
redis==5.2.1
celery==5.4.0

# --- Email / SMS Notifications ---
aiosmtplib==3.0.2
jinja2==3.1.4

# --- Cloud Integration ---
cloudinary==1.41.0
sendgrid==6.11.0

# --- Monitoring / Logging ---
loguru==0.7.3
